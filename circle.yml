machine:
  services:
    - docker

dependencies:
  pre:
    - sudo apt install jq     
    - pip install --upgrade pip
    - pip install awscli 

test:
  override:
    - mvn clean compile package

deployment:
  development:
    branch: ["cc-ecs"]
    commands:      
      - .deploy/ecs_deploy.sh DEV $CIRCLE_SHA1
  production:
    branch: master
    commands:
      - .deploy/dockerhub_push.sh PROD $CIRCLE_SHA1
      - .deploy/ebs_deploy.sh terms-service PROD $CIRCLE_SHA1



